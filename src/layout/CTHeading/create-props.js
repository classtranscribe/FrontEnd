 /**
 * The props for CTHeading component
 * @typedef {Object} CTHeadingProps
 * @property {String} as - One of `'h1', 'h2', 'h3', 'h4', 'h5'`
 * @property {String} heading - The Content of the heading
 * @property {Element} children - The Content of the heading
 * @property {String} icon - Icon name of the material-icons
 * @property {Boolean} sticky - The heading can be sticky
 * @property {Boolean} uppercase - The heading can be uppercase
 * @property {Boolean} gradient - The background of the heading can be gradient
 * @property {Number} offsetTop - Set the offset top of the sticky heading
 * @property {Boolean} highlight - The heading can be highlighted to teal color
 * @property {Boolean} highlightIcon - The icon can be highlighted to teal color
 * @property {Number|String|Number[]|String[]} padding - The padding in `x 10px`, allows 1,2,3,4,5
 */

/**
 * Create the props for CTHeading component
 * @param {CTHeadingProps} props - the props creator
 * @returns {CTHeadingProps} The props for CTHeading component
 */
export function createCTHeadingProps(props) {
  return props;
}
